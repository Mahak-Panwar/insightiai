"use client"

import React from 'react'
import { useForm } from 'react-hook-form';

const auditForm = () => {
     const {
    register,
    handleSubmit,
    formState: { errors },
  } = useForm();

  return (
    <div>
      
    </div>
  )
}

export default auditForm
